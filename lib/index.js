'use strict';

const compute = require('./objects/compute');
const storage = require('./objects/storage');
const connection = require('./objects/connection');
const application = require('./objects/application');

module.exports = {
  compute,
  storage,
  connection,
  application
};
